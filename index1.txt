<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>CultForge — Forge Reality</title>

    <meta name="description"
        content="CultForge — A next-gen community driven Minecraft launcher with realistic graphics, synced mods and missions." />

    <style>
        /* ================== ROOT ================== */
        :root {
            --bg: #05040a;
            --glass: rgba(255, 255, 255, .06);
            --border: rgba(255, 255, 255, .08);
            --neon: #00f0ff;
            --purple: #6f5aff;
            --pink: #ff6fcf;
            --gold: #ffb86b;
            --text: rgba(255, 255, 255, .88);
            --muted: rgba(255, 255, 255, .6);
            --radius: 16px;
        }

        /* ================== RESET ================== */
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0
        }

        html,
        body {
            height: 100%;
            scroll-behavior: smooth
        }

        body {
            background:
                radial-gradient(800px 500px at 10% 10%, rgba(111, 90, 255, .15), transparent 60%),
                radial-gradient(700px 400px at 90% 90%, rgba(0, 240, 255, .12), transparent 60%),
                var(--bg);
            color: var(--text);
            font-family: Inter, system-ui, Segoe UI, Roboto, Arial;
            overflow-x: hidden;
        }

        /* ================== STARFIELD ================== */
        canvas#stars {
            position: fixed;
            inset: 0;
            z-index: -3;
        }

        /* ================== SCANLINES ================== */
        body::after {
            content: "";
            position: fixed;
            inset: 0;
            background: repeating-linear-gradient(to bottom,
                    rgba(255, 255, 255, .02) 0px,
                    rgba(255, 255, 255, .02) 1px,
                    transparent 2px,
                    transparent 4px);
            mix-blend-mode: overlay;
            pointer-events: none;
            z-index: 2;
        }

        /* ================== HEADER ================== */
        header {
            position: fixed;
            top: 14px;
            left: 50%;
            transform: translateX(-50%);
            width: calc(100% - 28px);
            max-width: 1200px;
            padding: 12px 16px;
            border-radius: 14px;
            backdrop-filter: blur(12px);
            background: linear-gradient(180deg, rgba(255, 255, 255, .04), rgba(255, 255, 255, .02));
            border: 1px solid var(--border);
            display: flex;
            justify-content: space-between;
            align-items: center;
            gap: 14px;
            z-index: 50;
        }

        .brand {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .logo img {
            width: 38px;
            height: 38px;
            filter: drop-shadow(0 0 18px var(--neon));
        }

        .brand h1 {
            font-size: 13px;
            letter-spacing: .12em;
        }

        .brand p {
            font-size: 11px;
            color: var(--muted);
        }

        nav {
            display: flex;
            gap: 6px;
        }

        nav a {
            font-weight: 700;
            padding: 8px 10px;
            border-radius: 10px;
            transition: .3s;
        }

        nav a:hover {
            color: var(--neon);
            text-shadow: 0 0 12px var(--neon);
        }

        /* ================== BUTTON ================== */
        .btn {
            padding: 14px 18px;
            border-radius: 12px;
            font-weight: 900;
            cursor: pointer;
            border: none;
            color: #03030a;
            background: linear-gradient(90deg, var(--purple), var(--neon));
            box-shadow: 0 0 25px rgba(0, 240, 255, .35);
            transition: .25s;
            white-space: nowrap;
        }

        .btn:hover {
            transform: translateY(-3px) scale(1.03);
            box-shadow: 0 0 45px rgba(0, 240, 255, .7);
        }

        /* ================== HERO ================== */
        .hero {
            min-height: 90vh;
            display: grid;
            place-items: center;
            padding-top: 120px;
            padding-inline: 20px;
            text-align: center;
        }

        .hero h2 {
            font-size: clamp(32px, 8vw, 72px);
            line-height: 1.05;
        }

        /* GLITCH TEXT */
        .glitch {
            position: relative;
            display: inline-block;
        }

        .glitch::before,
        .glitch::after {
            content: attr(data-text);
            position: absolute;
            left: 0;
            top: 0;
            opacity: .75;
        }

        .glitch::before {
            color: var(--neon);
            clip-path: polygon(0 0, 100% 0, 100% 45%, 0 45%);
        }

        .glitch::after {
            color: var(--pink);
            clip-path: polygon(0 55%, 100% 55%, 100% 100%, 0 100%);
        }

        @keyframes gl1 {
            0% {
                transform: translate(0)
            }

            50% {
                transform: translate(2px, -2px)
            }
        }

        @keyframes gl2 {
            0% {
                transform: translate(0)
            }

            50% {
                transform: translate(-2px, 2px)
            }
        }

        .hero p {
            margin-top: 18px;
            color: var(--muted);
            max-width: 720px;
            margin-inline: auto;
        }

        /* ================== SECTIONS ================== */
        .section {
            padding: 80px 20px;
            max-width: 1200px;
            margin: auto;
        }

        .section h3 {
            margin-bottom: 22px;
            font-size: 22px;
        }

        /* ================== GRID ================== */
        .grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
            gap: 20px;
        }

        .card {
            background: var(--glass);
            border: 1px solid var(--border);
            border-radius: var(--radius);
            padding: 22px;
            backdrop-filter: blur(12px);
            box-shadow: 0 20px 60px rgba(0, 0, 0, .6);
            transition: .35s;
        }

        .card:hover {
            transform: translateY(-10px);
            box-shadow:
                0 0 40px rgba(0, 240, 255, .35),
                0 30px 80px rgba(0, 0, 0, .8);
        }

        /* ================== FOOTER ================== */
        footer {
            padding: 36px 18px;
            text-align: center;
            color: var(--muted);
        }

        /* ================== CURSOR ================== */
        .cursor {
            position: fixed;
            width: 22px;
            height: 22px;
            border-radius: 50%;
            background: radial-gradient(circle, var(--neon), transparent 60%);
            pointer-events: none;
            mix-blend-mode: screen;
            z-index: 100;
        }

        /* ================== MOBILE BREAKPOINTS ================== */
        @media (max-width: 768px) {
            header {
                flex-direction: column;
                align-items: flex-start;
            }

            nav {
                width: 100%;
                flex-wrap: wrap;
            }

            .btn {
                width: 100%;
                text-align: center;
            }
        }

        @media (max-width: 480px) {
            .hero {
                padding-top: 140px;
            }

            .logo img {
                width: 34px;
                height: 34px;
            }

            .brand h1 {
                font-size: 12px;
            }

            .cursor {
                display: none;
            }
        }
    </style>
</head>

<body>

    <canvas id="stars"></canvas>
    <div class="cursor" id="cursor"></div>

    <header>
        <div class="brand">
            <div class="logo"><img src="./static/images/main.png"></div>
            <div>
                <h1>CULTFORGE</h1>
                <p>A community based game launcher for minecraft.</p>
            </div>
        </div>

        <nav>
            <a href="#features">Features</a>
            <a href="#download">Download</a>
            <a href="#community">Community</a>
        </nav>

        <button class="btn">GET CULTFORGE</button>
    </header>

    <section class="hero">
        <div>
            <h2 class="glitch" data-text="CULTFORGE">CULTFORGE</h2>
            <p>A community-driven Minecraft launcher with realistic graphics, synced mods, missions and competitive
                worlds.</p>
            <div style="margin-top:28px">
                <button class="btn">DOWNLOAD NOW</button>
            </div>
        </div>
    </section>

    <section class="section" id="features">
        <h3>Core Systems</h3>
        <div class="grid">
            <div class="card">🔥 Community Leaderboards</div>
            <div class="card">⚔ Missions & Events</div>
            <div class="card">🌌 Synced Shaders & Mods</div>
            <div class="card">📡 Live Server Status</div>
            <div class="card">🧊 Cyber Glass UI</div>
            <div class="card">⚙ Auto Updates</div>
        </div>
    </section>

    <section class="section" id="download">
        <h3>Download</h3>
        <div class="grid">
            <div class="card">
                <h4>Stable Build</h4>
                <p class="muted">Recommended for all players.</p>
                <button class="btn">Windows x64</button>
            </div>
            <div class="card">
                <h4>Nightly</h4>
                <p class="muted">Experimental features.</p>
                <button class="btn">Nightly</button>
            </div>
        </div>
    </section>

    <section class="section" id="community">
        <h3>Community</h3>
        <div class="grid">
            <div class="card">💬 Discord</div>
            <div class="card">📢 WhatsApp</div>
            <div class="card">❤️ Donate</div>
        </div>
    </section>

    <footer>
        © 2025 CultForge — Forge Reality
    </footer>

    <script>
        /* ================== STARFIELD ================== */
        const c = document.getElementById("stars"), ctx = c.getContext("2d");
        let w, h, stars = [];
        function resize() {
            w = c.width = innerWidth;
            h = c.height = innerHeight;
            let count = innerWidth < 600 ? 80 : 160;
            stars = Array.from({ length: count }, () => ({
                x: Math.random() * w,
                y: Math.random() * h,
                z: Math.random() * 1.5 + .5
            }));
        }
        window.onresize = resize; resize();
        function draw() {
            ctx.clearRect(0, 0, w, h);
            ctx.fillStyle = "#fff";
            stars.forEach(s => {
                ctx.globalAlpha = s.z;
                ctx.fillRect(s.x, s.y, s.z * 2, s.z * 2);
                s.y += s.z * 0.5;
                if (s.y > h) s.y = 0;
            });
            requestAnimationFrame(draw);
        }
        draw();

        /* ================== CURSOR ================== */
        const cursor = document.getElementById("cursor");
        if (!("ontouchstart" in window)) {
            window.addEventListener("mousemove", e => {
                cursor.style.transform = `translate(${e.clientX - 11}px,${e.clientY - 11}px)`;
            });
        }
    </script>

</body>

</html>